defaults:
  - agent_kuhn_poker_fsp_train

# Benchmark: baseline 1 concurrent env
max_steps: 2
fsp_save_steps: 0
eval_steps: 10000
save_steps: 10000

train_env_manager:
  num_env_groups: 1
  group_size: 1
  max_env_num_per_worker: 64
  num_groups_partition: [1]
