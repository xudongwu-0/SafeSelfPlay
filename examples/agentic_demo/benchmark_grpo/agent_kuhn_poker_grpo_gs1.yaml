defaults:
  - agent_kuhn_poker_grpo_sweep

# group_size=1: no per-group baseline, rely on global whitening
exp_name: "kuhn_grpo_gs1"

train_env_manager:
  group_size: 1
  max_env_num_per_worker: 32

reward_normalization:
  grouping: tags
  norm_mean_type: ~

output_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs1
logging_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs1/logs
tracker_kwargs:
  log_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs1/tensorboard
checkpoint_config:
  type: file_system
  output_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs1/render
