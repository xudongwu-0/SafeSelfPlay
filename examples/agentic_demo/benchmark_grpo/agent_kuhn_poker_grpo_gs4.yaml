defaults:
  - agent_kuhn_poker_grpo_sweep

exp_name: "kuhn_grpo_gs4"

train_env_manager:
  group_size: 4
  max_env_num_per_worker: 128

reward_normalization:
  grouping: traj_group_id
  norm_mean_type: group

output_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs4
logging_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs4/logs
tracker_kwargs:
  log_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs4/tensorboard
checkpoint_config:
  type: file_system
  output_dir: /projects/bfoz/wchen11/kuhn_grpo_sweep/gs4/render
