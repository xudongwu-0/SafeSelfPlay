defaults:
  - ../config/traj_envs@_here_
  - ../config/deepspeed_zero@_here_
  - ../config/deepspeed_zero2@_here_
  - ../config/deepspeed_zero3@_here_
  - ../config/deepspeed_zero3_cpuoffload@_here_

hydra:
  run:
    dir: .
  output_subdir: null

exp_name: "smoke_kuhn_opp_reasoning"
seed: 42
logging_dir: /tmp/smoke_kuhn_opp_reasoning/logs
output_dir: /tmp/smoke_kuhn_opp_reasoning
system_envs: {}

track_with: stdout

checkpoint_config:
  type: file_system
  async_upload: false
  output_dir: /tmp/smoke_kuhn_opp_reasoning/render

num_gpus_per_node: 4

# 3 steps → 3 independent random seeds, ~88% chance of at least one P1 episode
max_steps: 3
save_steps: 10000
logging_steps: 1
eval_steps: 10000
resume_from_checkpoint: false
fsp_save_steps: 0           # no FSP: keep enemy_pool=[None] (base model only)
fsp_opponent_weight_mode: uniform
cold_start: false
async_generation_ratio: 1

# Small batch so logs are readable
rollout_batch_size: 4
val_batch_size: 1
sequence_length: 1024

advantage_clip: 10.0
ppo_epochs: 1
adv_estimator: "grpo"
init_kl_coef: 0.01
use_kl_loss: false
kl_loss_coef: 0.0
whiten_advantages: true
entropy_loss_coef: 0
max_grad_norm: 1.0

pretrain: Qwen/Qwen2.5-3B-Instruct
reward_pretrain: Qwen/Qwen2.5-3B-Instruct

# GPU 0: training (dp_size=1), GPUs 1-3: vLLM inference
actor_train:
  model_args:
    flash_attn: fa2
    disable_gradient_checkpointing: false
    dtype: bf16
    model_type: ~
    lora_target: q_proj,k_proj,v_proj,o_proj,gate_proj,up_proj,down_proj
    lora_rank: 32
    lora_alpha: 32
  training_args:
    learning_rate: 0.0          # no weight updates
    weight_decay: 0
    per_device_train_batch_size: 4
    gradient_accumulation_steps: 1
    warmup_steps: 0
    lr_scheduler_type: cosine
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: deepspeed_train
    strategy_config: ${deepspeed_zero2}
  device_mapping: "[0]"
  infer_batch_size: 4
  use_dynamic_batching_in_train: false
  max_tokens_per_microbatch_in_train: 0
  sequence_length_round_in_train: 4
  use_inner_dynamic_batching_in_train: false

actor_infer:
  model_args:
    flash_attn: sdpa
    disable_gradient_checkpointing: true
    dtype: bf16
    lora_target: q_proj,k_proj,v_proj,o_proj,gate_proj,up_proj,down_proj
    lora_rank: 32
    lora_alpha: 32
  generating_args:
    max_new_tokens: 512
    top_p: 0.99
    top_k: 100
    num_beams: 1
    temperature: 0.99
    num_return_sequences: 1
  data_args:
    template: qwen2_5
  strategy_args:
    strategy_name: vllm
    strategy_config:
      gpu_memory_utilization: 0.92
      block_size: 16
      load_format: auto
      max_loras: 2
      enforce_eager: false
      max_num_batched_tokens: 4096
  device_mapping: "[1,2,3]"
  infer_batch_size: 4

reward_normalization:
  grouping: tags
  method: identity

# 1 group, 4 samples — base model self-play, opponent reasoning + k-level reward
train_env_manager:
  format_penalty: -0.01
  max_env_num_per_worker: 4
  num_env_groups: 1
  group_size: 4
  tags: [KuhnPokerLLMThink]
  num_groups_partition: [1]
  include_opponent_reasoning: true

val_env_manager:
  max_env_num_per_worker: 1
  num_env_groups: 1
  group_size: 1
  tags: [KuhnPokerLLMThink]
  num_groups_partition: [1]

max_tokens_per_step: 512
max_actions_per_traj: 2

debug_log_group_rewards: true
debug_log_agent_response: true

custom_envs:
  KuhnPokerLLMThink:
    ${custom_env.KuhnPokerLLMThink}
