# @package _global_
defaults:
  - agent_kuhn_poker_bs_sweep

exp_name: "kuhn_bs2"

actor_train:
  training_args:
    per_device_train_batch_size: 2
    gradient_accumulation_steps: 16

output_dir: /projects/bfoz/wchen11/kuhn_bs_sweep/bs2
logging_dir: /projects/bfoz/wchen11/kuhn_bs_sweep/bs2/logs
tracker_kwargs:
  project: kuhn-bs-sweep
checkpoint_config:
  type: file_system
  output_dir: /projects/bfoz/wchen11/kuhn_bs_sweep/bs2/render
